apiVersion: v1
kind: Service
metadata:
  name: {{ include "uxa-job.fullname" . }}
  labels:
    {{- include "uxa-job.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "uxa-job.selectorLabels" . | nindent 4 }}
